# Design System: Steelab Landing Page

> **Source:** AI Gateway security landing page for Steelab — Vietnamese enterprise product.
> **PERSISTED:** Do not edit this file directly for page-level overrides. Use `pages/[page].md` instead.

---

## 1. Product Profile

| Field | Value |
|-------|-------|
| Product Type | Enterprise SaaS — AI Gateway Security |
| Industry | Cybersecurity / Compliance |
| Stack | HTML + Tailwind CSS (single static file) |
| Themes | Dark mode (default) + Light mode |

---

## 2. Pattern

**Name:** Enterprise Gateway
**Focus:** Trust signals, industry/role segmentation, compliance messaging, B2B conversion

- Logo carousel (client logos)
- Tab switching for industries
- Path selection (I am a...)
- Mega menu navigation
- Trust signals prominent (compliance badges, stats, testimonials)
- CTA Placement: Contact Sales (Primary) + Login (Secondary)

**Section Order:**
1. Hero (mission/video) → 2. Solutions by Industry → 3. Solutions by Role → 4. Client Logos → 5. Contact Sales CTA

---

## 3. Style

**Style Name:** Dark Cyber + Glassmorphism
**Keywords:** Dark mode, terminal aesthetic, cyberpunk elements, glassmorphism cards, HUD feel, professional enterprise

### Color Palette (Dark Mode — PRIMARY)

| Token | Hex | Tailwind |
|-------|-----|----------|
| `--bg-deep` | `#050a14` | `bg-slate-950` |
| `--bg-surface` | `#0c1322` | `bg-slate-900` |
| `--bg-surface-up` | `#111b2e` | `bg-slate-800` |
| `--bg-edge` | `#1e2d44` | `bg-slate-700` |
| `--text-body` | `#cbd5e1` | `text-slate-300` |
| `--text-muted` | `#64748b` | `text-slate-500` |
| Primary (Brand Blue) | `#2563eb` | `blue-600` |
| Accent (Gold) | `#c9a84c` | Custom gold |
| CTA (Green) | `#22c55e` | `green-500` |
| Alert Red | `#ef4444` | `red-500` |

### Color Palette (Light Mode — OVERRIDE)

| Token | Hex | Tailwind |
|-------|-----|----------|
| `--bg-deep` | `#f8fafc` | `bg-slate-50` |
| `--bg-surface` | `#ffffff` | `bg-white` |
| `--bg-surface-up` | `#f1f5f9` | `bg-slate-100` |
| `--bg-edge` | `#cbd5e1` | `bg-slate-300` |
| `--text-body` | `#334155` | `text-slate-700` |
| `--text-muted` | `#64748b` | `text-slate-500` |

### Cybersecurity Brand Colors (for accents)

| Role | Hex |
|------|-----|
| Matrix Green (security) | `#00FF41` |
| Terminal Dark | `#0D0D0D` |
| Cyber Cyan | `#00FFFF` |
| Alert Red | `#FF3333` |

### Effects

- **Glassmorphism cards:** `backdrop-blur-xl bg-white/[0.07] border border-white/10`
- **Neon glow (use sparingly):** `text-shadow: 0 0 10px #00FF41`
- **Terminal fonts:** JetBrains Mono (code), Be Vietnam Pro (body)
- **Serif headings:** Lora (headings) — existing, keep it

---

## 4. Typography

### Font Pairing: Corporate Trust + Existing Brand

| Role | Font | Source | Weight |
|------|------|--------|--------|
| Headings | Lora | Google Fonts | 400–700 |
| Body (Vietnamese) | Be Vietnam Pro | Google Fonts | 300–700 |
| Code/Terminal | JetBrains Mono | Google Fonts | 400–700 |

**Existing config — DO NOT CHANGE (already implemented):**
```html
<link href="https://fonts.googleapis.com/css2?family=Lora:wght@400;500;600;700&family=Be+Vietnam+Pro:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">
```

**Tailwind font family:**
```js
fontFamily: {
  heading: ['Lora', 'serif'],
  body: ['Be Vietnam Pro', 'sans-serif'],
  mono: ['JetBrains Mono', 'monospace'],
}
```

---

## 5. Key Effects

| Effect | CSS | Use Case |
|--------|-----|----------|
| Glassmorphism | `backdrop-blur-xl bg-white/[0.07] border border-white/10` | Cards, modals, nav |
| Card lift | `hover:-translate-y-1 hover:shadow-2xl` | Interactive cards |
| Scroll reveal | `opacity-0 translate-y-8` → `opacity-100 translate-y-0` | Section animations |
| Neon glow | `text-shadow: 0 0 15px currentColor` | Security highlights |
| Floating badge | `animate-float` keyframe | Hero badges |
| Marquee | `animate-marquee` keyframe | Logo carousel |

---

## 6. Anti-Patterns (AVOID)

- ❌ Matrix green used for large backgrounds (use sparingly as accent)
- ❌ Pure black `#000000` backgrounds — use `#050a14` (softer)
- ❌ Comic Sans / system fonts — keep Lora + Be Vietnam Pro
- ❌ Emojis as icons — use SVG only (Heroicons, Lucide, Material Symbols)
- ❌ `bg-white/10` glass in light mode — use `bg-white/80` or higher opacity
- ❌ Light mode muted text `#94A3B8` — use `#475569` (slate-600) minimum
- ❌ Instant state changes — always use `transition-all duration-200`
- ❌ `linear` easing — use `ease-out` for entering, `ease-in` for exiting
- ❌ Deep black borders in light mode — use `border-gray-200`

---

## 7. Pre-Delivery Checklist

### Visual Quality
- [ ] No emojis used as icons (use SVG instead)
- [ ] All icons from consistent icon set (Material Symbols Outlined)
- [ ] Hover states don't cause layout shift
- [ ] Use theme colors directly (e.g., `text-blue-600`) not `var()` wrapper

### Interaction
- [ ] All clickable elements have `cursor-pointer`
- [ ] Hover states provide clear visual feedback
- [ ] Transitions are smooth (150–300ms)
- [ ] Focus states visible for keyboard navigation

### Light/Dark Mode
- [ ] Light mode text has sufficient contrast (4.5:1 minimum)
- [ ] Glass/transparent elements visible in light mode
- [ ] Borders visible in both modes
- [ ] Test both modes before delivery

### Layout
- [ ] Floating navbar has `top-4 left-4 right-4` spacing
- [ ] No content hidden behind fixed navbars
- [ ] Responsive at 375px, 768px, 1024px, 1440px
- [ ] No horizontal scroll on mobile

### Accessibility
- [ ] All images have descriptive `alt` text
- [ ] Form inputs have associated `<label>` elements
- [ ] Color is not the only indicator
- [ ] `prefers-reduced-motion` media query respected

### Performance
- [ ] Images use WebP, `srcset`, `loading="lazy"`
- [ ] No infinite decorative animations (loading indicators only)
- [ ] No content jumping from async content

---

## 8. Stack Guidelines (html-tailwind)

### Tailwind Utilities to Use

```html
<!-- Glassmorphism card -->
<div class="backdrop-blur-xl bg-white/[0.07] border border-white/10 rounded-2xl">

<!-- Floating navbar -->
<nav class="fixed top-4 left-4 right-4 z-50 ...">

<!-- Scroll reveal -->
<div class="reveal opacity-0 translate-y-8 transition-all duration-700 ease-out">

<!-- Trust badge -->
<span class="inline-flex items-center gap-2 text-xs text-green-400 ...">
```

### Common Tailwind Classes Used in This Project

| Pattern | Class |
|---------|-------|
| Section padding | `py-20 lg:py-32` |
| Container | `max-w-6xl mx-auto px-4 sm:px-6 lg:px-8` |
| Section divider | `border-t border-white/5` |
| Card | `bg-white/[0.07] backdrop-blur-xl border border-white/10 rounded-2xl` |
| Badge | `inline-flex items-center gap-1.5 px-3 py-1 text-xs font-medium rounded-full` |
| CTA Button | `px-6 py-3 bg-blue-600 hover:bg-blue-500 text-white rounded-xl font-semibold transition-all duration-200` |

---

## 9. References

- Design source: UI/UX Pro Max — `ui-ux-pro-max` Claude plugin
- Color theory: Cybersecurity dark theme palette (Matrix Green + Deep Navy)
- Style: Cyberpunk UI + Glassmorphism hybrid
- Font: Lora (headings) + Be Vietnam Pro (body) — existing, do not change
